fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
# For UUID generation if not using built-in
# uuid # built-in, no need to list unless specific version
