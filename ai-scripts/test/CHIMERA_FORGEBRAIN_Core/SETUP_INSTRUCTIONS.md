This relic defines the core cognitive architecture of the CHIMERA_FORGEBRAIN AI itself. It should be stored in a dedicated Git repository and used as the foundational context for initializing the AI session.

1.  **Materialize the Relic:** Run `make up` after the `relic-forge-bay-api` has processed this plan.
2.  **Version Control:** Commit this entire directory to a new Git repository named `chimera-forgebrain-core`.
3.  **Integration:** The AI's C++ host application should be configured to load these YAML files at startup to establish its core operational context, persona, and knowledge base. This is the new genesis block.