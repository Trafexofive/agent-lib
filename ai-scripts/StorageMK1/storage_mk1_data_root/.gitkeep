# This directory is mounted into the Docker container as the main storage area.
# Files and folders created via the StorageMK1 API will appear here on the host system.
# Ensure STORAGE_ROOT_HOST_PATH in .env points to this directory (or your desired absolute path).